package cmd

// Install takes the passed code file, resolves its dependencies, emits an executable and installes it to a directory in the system PATH
func Install(sourceFile string) {
	// Compile program and emit executable to local bin dir

}
