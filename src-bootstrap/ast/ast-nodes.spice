// Std imports
import "std/data/vector";
import "std/type/any";

// Own imports
import "bootstrap/ast/abstract-ast-visitor-intf";
import "bootstrap/model/function-intf";
import "bootstrap/model/struct-intf";
import "bootstrap/model/interface-intf";
import "bootstrap/reader/code-loc";
import "bootstrap/symboltablebuilder/type-qualifiers";
import "bootstrap/symboltablebuilder/symbol-table-entry";
import "bootstrap/symboltablebuilder/scope-intf";
import "bootstrap/ast/attributes";
//import "bootstrap/symboltablebuilder/qual-type";
//import "bootstrap/model/function";

// Access tokens used when building type/function/scope signatures
public const string MEMBER_ACCESS_TOKEN = ".";
public const string SCOPE_ACCESS_TOKEN = "::";

// Operator overload function names
public const string OP_FCT_PREFIX = "op.";
public const string OP_FCT_PLUS = "op.plus";
public const string OP_FCT_MINUS = "op.minus";
public const string OP_FCT_MUL = "op.mul";
public const string OP_FCT_DIV = "op.div";
public const string OP_FCT_EQUAL = "op.equal";
public const string OP_FCT_NOT_EQUAL = "op.notequal";
public const string OP_FCT_SHL = "op.shl";
public const string OP_FCT_SHR = "op.shr";
public const string OP_FCT_BITWISE_AND = "op.bitwiseand";
public const string OP_FCT_BITWISE_OR = "op.bitwiseor";
public const string OP_FCT_BITWISE_XOR = "op.bitwisexor";
public const string OP_FCT_BITWISE_NOT = "op.bitwisenot";
public const string OP_FCT_PLUS_EQUAL = "op.plusequal";
public const string OP_FCT_MINUS_EQUAL = "op.minusequal";
public const string OP_FCT_MUL_EQUAL = "op.mulequal";
public const string OP_FCT_DIV_EQUAL = "op.divequal";
public const string OP_FCT_POSTFIX_PLUS_PLUS = "op.plusplus.post";
public const string OP_FCT_POSTFIX_MINUS_MINUS = "op.minusminus.post";
public const string OP_FCT_SUBSCRIPT = "op.subscript";
public const string OP_FCT_ASSIGN = "op.assign";

/**
 * Saves a constant value for an AST node to realize features like array-out-of-bounds checks
 */
public type CompileTimeValue union {
    public double doubleValue
    public int intValue
    public short shortValue
    public long longValue
    public byte byteValue
    public char charValue
    public unsigned long stringValueOffset // Offset into vector of strings in GlobalResourceManager
    public bool boolValue
}

public type IASTNode interface {
    public f<Any> accept(IAbstractAstVisitor*);
    public f<Vector<IASTNode*>> getChildren();
    public f<Vector<Vector<const IFunction*>>> getOpFctPointers();
    public p customItemsInitialization(unsigned long /*manifestationCount*/);
    public f<bool> hasCompileTimeValue();
    public f<CompileTimeValue> getCompileTimeValue();
    public f<bool> returnsOnAllControlPaths(bool* /*doSetPredecessorsUnreachable*/);
    public f<Vector<IFunction*>*> getFctManifestations(const String&);
    public f<Vector<IStruct*>*> getStructManifestations();
    public f<Vector<IInterface*>*> getInterfaceManifestations();
    public f<bool> isFctOrProcDef();
    public f<bool> isStructDef();
    public f<bool> isParam();
    public f<bool> isStmtLst();
    public f<bool> isAssignExpr();
    public f<bool> isExprStmt();
    public f<const CodeLoc&> getCodeLoc();
}

public type IVisitable interface {
    public f<Any> accept(IAbstractAstVisitor*);
}

// =========================================================== ASTNode ===========================================================

public type ASTNode struct/*: IVisitable*/ {
    public ASTNode* parent = nil<ASTNode*>
    public Vector<ASTNode*> children
    public CodeLoc codeLoc
}

public p ASTNode.ctor(const CodeLoc& codeLoc) {
    this.codeLoc = codeLoc;
}

// Explicit destructor. AST nodes only ever live inside the BlockAllocator pool and are released via
// sDestruct(ASTNode*), so their destructor is never reached through normal value destruction. Defining
// it explicitly makes sure it is generated. The composed 'children' vector only holds non-owning
// pointers, so it is enough to let the implicitly appended member destruction free the vector storage.
public p ASTNode.dtor() {}

public f<Any> ASTNode.accept(IAbstractAstVisitor* _) {
    assert false; // Concrete node types must override accept()
    return Any();
}

// Register a child node and set its parent to this node.
public p ASTNode.addChild(ASTNode* child) {
    assert child != nil<ASTNode*>;
    child.parent = this;
    this.children.pushBack(child);
}

public f<const Vector<ASTNode*>&> ASTNode.getChildren() {
    return this.children;
}

public p ASTNode.resizeToNumberOfManifestations(unsigned long manifestationCount) {
    // Resize children
    foreach ASTNode* child : this.children {
        assert child != nil<ASTNode*>;
        child.resizeToNumberOfManifestations(manifestationCount);
    }
    // Do custom work (override in subclasses to add per-node behavior)
}

// ========================================================== EntryNode ==========================================================

public type ASTEntryNode struct {
    compose public ASTNode node

    public Vector<IVisitable*> modAttrs
    public Vector<IVisitable*> importDefs
    public Vector<IVisitable*> topLevelDefs
}

public p ASTEntryNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTEntryNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitEntry(this);
}

// ======================================================= TopLevelDefNode =======================================================

public type ASTTopLevelDefNode struct/*: IVisitable*/ {
    compose public ASTNode node
}

public p ASTTopLevelDefNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}

// =========================================================== StmtNode ==========================================================

public type ASTStmtNode struct/*: IVisitable*/ {
    compose public ASTNode node
    public bool unreachable = false
}

public p ASTStmtNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}

// =========================================================== ExprNode ==========================================================

public type ASTExprNode struct/*: IVisitable*/ {
    compose public ASTNode node
    // ToDo: QualTypeList symbolTypes (blocked on qual-type.spice compiling)
}

public p ASTExprNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}

// The node types below are declared here, ahead of the top-level-def/stmt nodes that reference them,
// because Spice requires a struct/interface to be defined before use within the same file (no forward
// declarations). Each one is either a leaf (no struct-pointer fields of its own) or only references
// other nodes already defined above/within this same block.

// ========================================================== ConstantNode =======================================================

public type ASTConstantNode struct : IVisitable {
    compose public ASTExprNode node
    public CompileTimeValue compileTimeValue
}

public p ASTConstantNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTConstantNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitConstant(this);
}

// ============================================================ AttrNode =========================================================

public type ASTAttrNode struct {
    compose public ASTNode node
    public String key
    // Defaults to TARGET_INVALID / ATTR_TYPE_INVALID (both enum ordinal 0) without naming the enum value
    // here: attributes.spice imports this file, so resolving AttrTarget::TARGET_INVALID as a field default
    // can hit a 'used before declared' error depending on which file is the compile entry point.
    public AttrTarget target
    public AttrType attrType
    public ASTConstantNode* value = nil<ASTConstantNode*>
}

public p ASTAttrNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTAttrNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitAttr(this);
}

// Returns the attribute's compile-time value, or nil if it was declared without one (bool attributes
// default to 'true' when omitted; callers apply that default themselves, matching the host).
public f<const CompileTimeValue*> ASTAttrNode.getValue() {
    if this.value == nil<ASTConstantNode*> {
        return nil<const CompileTimeValue*>;
    }
    return &this.value.compileTimeValue;
}

// ========================================================== AttrLstNode ========================================================

public type ASTAttrLstNode struct {
    compose public ASTNode node
    public Vector<ASTAttrNode*> attributes
}

public p ASTAttrLstNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTAttrLstNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitAttrLst(this);
}

public f<bool> ASTAttrLstNode.hasAttr(const String& key) {
    foreach const ASTAttrNode* attr : this.attributes {
        if attr.key == key {
            return true;
        }
    }
    return false;
}

// Returns the attribute's values by copy (CompileTimeValue is a small, trivially-copyable union), so
// there is no shared/static-storage lifetime to manage for the synthesized default-bool fallback below.
public f<Vector<CompileTimeValue>> ASTAttrLstNode.getAttrValuesByName(const String& key) {
    Vector<CompileTimeValue> attributeValues;
    foreach const ASTAttrNode* attr : this.attributes {
        if attr.key != key {
            continue;
        }
        const CompileTimeValue* value = attr.getValue();
        if value == nil<const CompileTimeValue*> {
            // Attribute given without a value defaults to boolValue = true (bool attributes only)
            CompileTimeValue defaultValue;
            defaultValue.boolValue = true;
            attributeValues.pushBack(defaultValue);
        } else {
            attributeValues.pushBack(*value);
        }
    }
    return attributeValues;
}

// Precondition: hasAttr(key) must be true (matches every call site in ImportCollector/SymbolTableBuilder,
// which always guard with hasAttr first).
public f<CompileTimeValue> ASTAttrLstNode.getAttrValueByName(const String& key) {
    const Vector<CompileTimeValue> values = this.getAttrValuesByName(key);
    assert !values.isEmpty();
    return values.get(values.getSize() - 1l);
}

// ========================================================= QualifierNode =======================================================

type QualifierType enum {
    NONE,
    CONST,
    SIGNED,
    UNSIGNED,
    INLINE,
    PUBLIC,
    HEAP,
    COMPOSE
}

public type ASTQualifierNode struct {
    compose public ASTNode node
    public QualifierType qualifierType = QualifierType::NONE
}

public p ASTQualifierNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTQualifierNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitQualifier(this);
}

// ======================================================== QualifierLstNode =====================================================

public type ASTQualifierLstNode struct {
    compose public ASTNode node
    public Vector<ASTQualifierNode*> qualifiers
}

public p ASTQualifierLstNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTQualifierLstNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitQualifierLst(this);
}

// ======================================================= CustomDataTypeNode ====================================================

public type ASTCustomDataTypeNode struct : IVisitable {
    compose public ASTExprNode node
    public Vector<String> typeNameFragments
}

public p ASTCustomDataTypeNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTCustomDataTypeNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitCustomDataType(this);
}

// ======================================================== BaseDataTypeNode =====================================================

public type ASTBaseDataTypeNode struct : IVisitable {
    compose public ASTExprNode node
    public ASTCustomDataTypeNode* customDataType = nil<ASTCustomDataTypeNode*>
}

public p ASTBaseDataTypeNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTBaseDataTypeNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitBaseDataType(this);
}

// ========================================================== DataTypeNode =======================================================

public type ASTDataTypeNode struct : IVisitable {
    compose public ASTExprNode node
    public ASTBaseDataTypeNode* baseDataType = nil<ASTBaseDataTypeNode*>
}

public p ASTDataTypeNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTDataTypeNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitDataType(this);
}

// ========================================================== TypeLstNode ========================================================

public type ASTTypeLstNode struct {
    compose public ASTNode node
    public Vector<ASTDataTypeNode*> dataTypes
}

public p ASTTypeLstNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTTypeLstNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitTypeLst(this);
}

// ======================================================== MainFctDefNode =======================================================

public type ASTMainFctDefNode struct : IVisitable {
    compose public ASTTopLevelDefNode node

    public IVisitable* attrs = nil<IVisitable*>
    public IVisitable* paramLst = nil<IVisitable*>
    public IVisitable* body = nil<IVisitable*>
    public bool takesArgs = false
    public SymbolTableEntry* entry = nil<SymbolTableEntry*>
    public IScope* bodyScope = nil<IScope*>
}

public p ASTMainFctDefNode.ctor(CodeLoc codeLoc) {
    this.node = ASTTopLevelDefNode(codeLoc);
}
public f<Any> ASTMainFctDefNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitMainFctDef(this);
}

public f<String> ASTMainFctDefNode.getScopeId() {
    return String("fct:main");
}


// ========================================================= FctNameNode =========================================================

public type ASTFctNameNode struct {
    compose public ASTNode node

    public String name
    public String structName
    public String fqName
    public Vector<String> nameFragments
}

public p ASTFctNameNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTFctNameNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitFctName(this);
}


// ======================================================== FctDefBaseNode =======================================================

public type ASTFctDefBaseNode struct/*: IVisitable*/ {
    compose public ASTTopLevelDefNode node

    public IVisitable* attrs = nil<IVisitable*>
    public ASTQualifierLstNode* qualifierLst = nil<ASTQualifierLstNode*>
    public ASTFctNameNode* name = nil<ASTFctNameNode*>
    public bool isMethod = false
    public bool hasTemplateTypes = false
    public bool hasParams = false
    public TypeQualifiers qualifiers
    public IVisitable* paramLst = nil<IVisitable*>
    public IVisitable* body = nil<IVisitable*>
    public SymbolTableEntry* entry = nil<SymbolTableEntry*>
    public IScope* structScope = nil<IScope*>
    public IScope* scope = nil<IScope*>
}

public p ASTFctDefBaseNode.ctor(CodeLoc codeLoc) {
    this.node = ASTTopLevelDefNode(codeLoc);
}

// ========================================================== FctDefNode =========================================================

public type ASTFctDefNode struct : IVisitable {
    compose public ASTFctDefBaseNode node

}

public p ASTFctDefNode.ctor(CodeLoc codeLoc) {
    this.node = ASTFctDefBaseNode(codeLoc);
}
public f<Any> ASTFctDefNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitFctDef(this);
}

public f<String> ASTFctDefNode.getScopeId() {
    return String("fct:") + this.codeLoc.toString();
}


// ========================================================== ProcDefNode ========================================================

public type ASTProcDefNode struct : IVisitable {
    compose public ASTFctDefBaseNode node

    public bool isCtor = false
}

public p ASTProcDefNode.ctor(CodeLoc codeLoc) {
    this.node = ASTFctDefBaseNode(codeLoc);
}
public f<Any> ASTProcDefNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitProcDef(this);
}

public f<String> ASTProcDefNode.getScopeId() {
    return String("proc:") + this.codeLoc.toString();
}


// ========================================================= StructDefNode =======================================================

public type ASTStructDefNode struct : IVisitable {
    compose public ASTTopLevelDefNode node

    public IVisitable* attrs = nil<IVisitable*>
    public String structName
    public bool hasTemplateTypes = false
    public bool hasInterfaces = false
    public ASTTypeLstNode* interfaceTypeLst = nil<ASTTypeLstNode*>
    public Vector<IVisitable*> fields
    public ASTQualifierLstNode* qualifierLst = nil<ASTQualifierLstNode*>
    public TypeQualifiers qualifiers
    public SymbolTableEntry* entry = nil<SymbolTableEntry*>
    public unsigned long typeId
    public IScope* structScope = nil<IScope*>
}

public p ASTStructDefNode.ctor(CodeLoc codeLoc) {
    this.node = ASTTopLevelDefNode(codeLoc);
}
public f<Any> ASTStructDefNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitStructDef(this);
}


// ======================================================== InterfaceDefNode =====================================================

public type ASTInterfaceDefNode struct : IVisitable {
    compose public ASTTopLevelDefNode node

    public IVisitable* attrs = nil<IVisitable*>
    public String interfaceName
    public Vector<IVisitable*> signatures
    public ASTQualifierLstNode* qualifierLst = nil<ASTQualifierLstNode*>
    public TypeQualifiers qualifiers
    public SymbolTableEntry* entry = nil<SymbolTableEntry*>
    public unsigned long typeId
    public IScope* interfaceScope = nil<IScope*>
}

public p ASTInterfaceDefNode.ctor(CodeLoc codeLoc) {
    this.node = ASTTopLevelDefNode(codeLoc);
}
public f<Any> ASTInterfaceDefNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitInterfaceDef(this);
}


// ========================================================== UnionDefNode =======================================================

public type ASTUnionDefNode struct : IVisitable {
    compose public ASTTopLevelDefNode node

    public IVisitable* attrs = nil<IVisitable*>
    public String unionName
    public bool hasTemplateTypes = false
    public Vector<IVisitable*> fields
    public ASTQualifierLstNode* qualifierLst = nil<ASTQualifierLstNode*>
    public TypeQualifiers qualifiers
    public SymbolTableEntry* entry = nil<SymbolTableEntry*>
    public unsigned long typeId
    public IScope* unionScope = nil<IScope*>
}

public p ASTUnionDefNode.ctor(CodeLoc codeLoc) {
    this.node = ASTTopLevelDefNode(codeLoc);
}
public f<Any> ASTUnionDefNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitUnionDef(this);
}


// ========================================================== EnumDefNode ========================================================

public type ASTEnumDefNode struct : IVisitable {
    compose public ASTTopLevelDefNode node

    public String enumName
    public IVisitable* itemLst = nil<IVisitable*>
    public ASTQualifierLstNode* qualifierLst = nil<ASTQualifierLstNode*>
    public TypeQualifiers qualifiers
    public SymbolTableEntry* entry = nil<SymbolTableEntry*>
    public unsigned long typeId
    public IScope* enumScope = nil<IScope*>
}

public p ASTEnumDefNode.ctor(CodeLoc codeLoc) {
    this.node = ASTTopLevelDefNode(codeLoc);
}
public f<Any> ASTEnumDefNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitEnumDef(this);
}


// ======================================================= GenericTypeDefNode ====================================================

public type ASTGenericTypeDefNode struct : IVisitable {
    compose public ASTTopLevelDefNode node

    public String typeName
    public SymbolTableEntry* entry = nil<SymbolTableEntry*>
}

public p ASTGenericTypeDefNode.ctor(CodeLoc codeLoc) {
    this.node = ASTTopLevelDefNode(codeLoc);
}
public f<Any> ASTGenericTypeDefNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitGenericTypeDef(this);
}


// ========================================================== AliasDefNode =======================================================

public type ASTAliasDefNode struct : IVisitable {
    compose public ASTTopLevelDefNode node

    public ASTQualifierLstNode* qualifierLst = nil<ASTQualifierLstNode*>
    public TypeQualifiers qualifiers
    public String aliasName
    public unsigned long typeId
    public SymbolTableEntry* entry = nil<SymbolTableEntry*>
    public SymbolTableEntry* aliasedTypeContainerEntry = nil<SymbolTableEntry*>
}

public p ASTAliasDefNode.ctor(CodeLoc codeLoc) {
    this.node = ASTTopLevelDefNode(codeLoc);
}
public f<Any> ASTAliasDefNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitAliasDef(this);
}


// ======================================================== GlobalVarDefNode =====================================================

public type ASTGlobalVarDefNode struct : IVisitable {
    compose public ASTTopLevelDefNode node

    public String varName
    public SymbolTableEntry* entry = nil<SymbolTableEntry*>
}

public p ASTGlobalVarDefNode.ctor(CodeLoc codeLoc) {
    this.node = ASTTopLevelDefNode(codeLoc);
}
public f<Any> ASTGlobalVarDefNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitGlobalVarDef(this);
}


// ========================================================== ExtDeclNode ========================================================

public type ASTExtDeclNode struct : IVisitable {
    compose public ASTTopLevelDefNode node

    public IVisitable* attrs = nil<IVisitable*>
    public String extFunctionName
    public bool hasReturnType = false
    public SymbolTableEntry* entry = nil<SymbolTableEntry*>
}

public p ASTExtDeclNode.ctor(CodeLoc codeLoc) {
    this.node = ASTTopLevelDefNode(codeLoc);
}
public f<Any> ASTExtDeclNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitExtDecl(this);
}

public f<String> ASTExtDeclNode.getScopeId() {
    return (this.hasReturnType ? String("func:") : String("proc:")) + this.codeLoc.toString();
}


// ========================================================= ImportDefNode =======================================================

public type ASTImportDefNode struct : IVisitable {
    compose public ASTTopLevelDefNode node

    public String importPath
    public String importName
    public SymbolTableEntry* entry = nil<SymbolTableEntry*>
}

public p ASTImportDefNode.ctor(CodeLoc codeLoc) {
    this.node = ASTTopLevelDefNode(codeLoc);
}
public f<Any> ASTImportDefNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitImportDef(this);
}


// ========================================================== UnsafeBlockNode ========================================================

public type ASTUnsafeBlockNode struct : IVisitable {
    compose public ASTStmtNode node

    public IVisitable* body = nil<IVisitable*>
    public IScope* bodyScope = nil<IScope*>
}

public p ASTUnsafeBlockNode.ctor(CodeLoc codeLoc) {
    this.node = ASTStmtNode(codeLoc);
}
public f<Any> ASTUnsafeBlockNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitUnsafeBlock(this);
}

public f<String> ASTUnsafeBlockNode.getScopeId() {
    return String("unsafe:") + this.codeLoc.toString();
}


// ========================================================== ForLoopNode ========================================================

public type ASTForLoopNode struct : IVisitable {
    compose public ASTStmtNode node

    public IVisitable* initDecl = nil<IVisitable*>
    public IVisitable* condAssign = nil<IVisitable*>
    public IVisitable* body = nil<IVisitable*>
    public IScope* bodyScope = nil<IScope*>
}

public p ASTForLoopNode.ctor(CodeLoc codeLoc) {
    this.node = ASTStmtNode(codeLoc);
}
public f<Any> ASTForLoopNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitForLoop(this);
}

public f<String> ASTForLoopNode.getScopeId() {
    return String("for:") + this.codeLoc.toString();
}


// ======================================================== ForeachLoopNode ======================================================

public type ASTForeachLoopNode struct : IVisitable {
    compose public ASTStmtNode node

    public IVisitable* idxVarDecl = nil<IVisitable*>
    public IVisitable* itemVarDecl = nil<IVisitable*>
    public IVisitable* body = nil<IVisitable*>
    public IScope* bodyScope = nil<IScope*>
}

public p ASTForeachLoopNode.ctor(CodeLoc codeLoc) {
    this.node = ASTStmtNode(codeLoc);
}
public f<Any> ASTForeachLoopNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitForeachLoop(this);
}

public f<String> ASTForeachLoopNode.getScopeId() {
    return String("foreach:") + this.codeLoc.toString();
}


// ========================================================= WhileLoopNode =======================================================

public type ASTWhileLoopNode struct : IVisitable {
    compose public ASTStmtNode node

    public IVisitable* condition = nil<IVisitable*>
    public IVisitable* body = nil<IVisitable*>
    public IScope* bodyScope = nil<IScope*>
}

public p ASTWhileLoopNode.ctor(CodeLoc codeLoc) {
    this.node = ASTStmtNode(codeLoc);
}
public f<Any> ASTWhileLoopNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitWhileLoop(this);
}

public f<String> ASTWhileLoopNode.getScopeId() {
    return String("while:") + this.codeLoc.toString();
}


// ======================================================== DoWhileLoopNode ======================================================

public type ASTDoWhileLoopNode struct : IVisitable {
    compose public ASTStmtNode node

    public IVisitable* condition = nil<IVisitable*>
    public IVisitable* body = nil<IVisitable*>
    public IScope* bodyScope = nil<IScope*>
}

public p ASTDoWhileLoopNode.ctor(CodeLoc codeLoc) {
    this.node = ASTStmtNode(codeLoc);
}
public f<Any> ASTDoWhileLoopNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitDoWhileLoop(this);
}

public f<String> ASTDoWhileLoopNode.getScopeId() {
    return String("dowhile:") + this.codeLoc.toString();
}


// ========================================================== IfStmtNode ========================================================

public type ASTIfStmtNode struct : IVisitable {
    compose public ASTStmtNode node

    public IVisitable* condition = nil<IVisitable*>
    public IVisitable* thenBody = nil<IVisitable*>
    public IScope* thenBodyScope = nil<IScope*>
    public IVisitable* elseStmt = nil<IVisitable*>
}

public p ASTIfStmtNode.ctor(CodeLoc codeLoc) {
    this.node = ASTStmtNode(codeLoc);
}
public f<Any> ASTIfStmtNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitIfStmt(this);
}

public f<String> ASTIfStmtNode.getScopeId() {
    return String("if:") + this.codeLoc.toString();
}


// ========================================================== ElseStmtNode =======================================================

public type ASTElseStmtNode struct : IVisitable {
    compose public ASTStmtNode node

    public bool isElseIf = false
    public IVisitable* ifStmt = nil<IVisitable*>
    public IVisitable* body = nil<IVisitable*>
    public IScope* elseBodyScope = nil<IScope*>
}

public p ASTElseStmtNode.ctor(CodeLoc codeLoc) {
    this.node = ASTStmtNode(codeLoc);
}
public f<Any> ASTElseStmtNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitElseStmt(this);
}

public f<String> ASTElseStmtNode.getScopeId() {
    return String("if:") + this.codeLoc.toString();
}


// ========================================================= SwitchStmtNode ======================================================

public type ASTSwitchStmtNode struct : IVisitable {
    compose public ASTStmtNode node

}

public p ASTSwitchStmtNode.ctor(CodeLoc codeLoc) {
    this.node = ASTStmtNode(codeLoc);
}
public f<Any> ASTSwitchStmtNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitSwitchStmt(this);
}


// ========================================================= CaseBranchNode ======================================================

public type ASTCaseBranchNode struct : IVisitable {
    compose public ASTNode node

    public IVisitable* body = nil<IVisitable*>
    public IScope* bodyScope = nil<IScope*>
}

public p ASTCaseBranchNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTCaseBranchNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitCaseBranch(this);
}

public f<String> ASTCaseBranchNode.getScopeId() {
    return String("case:") + this.codeLoc.toString();
}


// ======================================================== DefaultBranchNode ====================================================

public type ASTDefaultBranchNode struct : IVisitable {
    compose public ASTNode node

    public IVisitable* body = nil<IVisitable*>
    public IScope* bodyScope = nil<IScope*>
}

public p ASTDefaultBranchNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTDefaultBranchNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitDefaultBranch(this);
}

public f<String> ASTDefaultBranchNode.getScopeId() {
    return String("default:") + this.codeLoc.toString();
}


// ===================================================== AnonymousBlockStmtNode ==================================================

public type ASTAnonymousBlockStmtNode struct : IVisitable {
    compose public ASTStmtNode node

    public IVisitable* body = nil<IVisitable*>
    public IScope* bodyScope = nil<IScope*>
}

public p ASTAnonymousBlockStmtNode.ctor(CodeLoc codeLoc) {
    this.node = ASTStmtNode(codeLoc);
}
public f<Any> ASTAnonymousBlockStmtNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitAnonymousBlockStmt(this);
}

public f<String> ASTAnonymousBlockStmtNode.getScopeId() {
    return String("anon:") + this.codeLoc.toString();
}


// ========================================================== StmtLstNode ========================================================

public type ASTStmtLstNode struct : IVisitable {
    compose public ASTNode node
    public Vector<IVisitable*> stmts
}

public p ASTStmtLstNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTStmtLstNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitStmtLst(this);
}


// ==================================================== TypeLstWithEllipsisNode ==================================================

public type ASTTypeLstWithEllipsisNode struct {
    compose public ASTNode node

}

public p ASTTypeLstWithEllipsisNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTTypeLstWithEllipsisNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitTypeLstWithEllipsis(this);
}


// ======================================================== TypeAltsLstNode ======================================================

public type ASTTypeAltsLstNode struct {
    compose public ASTNode node

}

public p ASTTypeAltsLstNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTTypeAltsLstNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitTypeAltsLst(this);
}


// ========================================================== DeclStmtNode =======================================================
// Declared before ParamLstNode (which composes a Vector<ASTDeclStmtNode*> below) since parseParamLst
// back-annotates each param's 'isFctParam' field directly (not via dispatch), so the list needs to stay
// concretely typed, which in turn needs DeclStmtNode defined first (same-file ordering rule).

public type ASTDeclStmtNode struct : IVisitable {
    compose public ASTStmtNode node

    public String varName
    public bool hasAssignment = false
    public IVisitable* assignExpr = nil<IVisitable*>
    public bool isFctParam = false
}

public p ASTDeclStmtNode.ctor(CodeLoc codeLoc) {
    this.node = ASTStmtNode(codeLoc);
}
public f<Any> ASTDeclStmtNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitDeclStmt(this);
}


// ========================================================== ParamLstNode =======================================================

public type ASTParamLstNode struct : IVisitable {
    compose public ASTNode node
    public Vector<ASTDeclStmtNode*> params
}

public p ASTParamLstNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTParamLstNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitParamLst(this);
}


// =========================================================== ArgLstNode ========================================================

public type ASTArgLstNode struct {
    compose public ASTNode node

}

public p ASTArgLstNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTArgLstNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitArgLst(this);
}


// ========================================================== EnumItemNode =======================================================
// Declared before EnumItemLstNode (which composes a Vector<ASTEnumItemNode*> below) since parseEnumDef
// back-links each item's 'enumDef' field directly (not via dispatch), so the list needs to stay
// concretely typed, which in turn needs EnumItemNode defined first (same-file ordering rule).

public type ASTEnumItemNode struct {
    compose public ASTNode node

    public String itemName
    public bool hasValue = false
    public unsigned int itemValue
    public SymbolTableEntry* entry = nil<SymbolTableEntry*>
    public ASTEnumDefNode* enumDef = nil<ASTEnumDefNode*>
}

public p ASTEnumItemNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTEnumItemNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitEnumItem(this);
}


// ======================================================== EnumItemLstNode ======================================================

public type ASTEnumItemLstNode struct : IVisitable {
    compose public ASTNode node
    public Vector<ASTEnumItemNode*> items
}

public p ASTEnumItemLstNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTEnumItemLstNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitEnumItemLst(this);
}


// =========================================================== FieldNode =========================================================

public type ASTFieldNode struct : IVisitable {
    compose public ASTNode node
    public String fieldName
}

public p ASTFieldNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTFieldNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitField(this);
}


// ========================================================= SignatureNode =======================================================

public type ASTSignatureNode struct : IVisitable {
    compose public ASTNode node

    public ASTQualifierLstNode* qualifierLst = nil<ASTQualifierLstNode*>
    public TypeQualifiers signatureQualifiers
    public String methodName
    public SymbolTableEntry* entry = nil<SymbolTableEntry*>
}

public p ASTSignatureNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTSignatureNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitSignature(this);
}


// ========================================================== DeclStmtNode =======================================================

// ========================================================== ExprStmtNode =======================================================

public type ASTExprStmtNode struct : IVisitable {
    compose public ASTStmtNode node

    public IVisitable* expr = nil<IVisitable*>
}

public p ASTExprStmtNode.ctor(CodeLoc codeLoc) {
    this.node = ASTStmtNode(codeLoc);
}
public f<Any> ASTExprStmtNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitExprStmt(this);
}


// ========================================================== ModAttrNode ========================================================

public type ASTModAttrNode struct : IVisitable {
    compose public ASTNode node
    public ASTAttrLstNode* attrLst = nil<ASTAttrLstNode*>
}

public p ASTModAttrNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTModAttrNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitModAttr(this);
}


// =================================================== TopLevelDefAttrNode ================================================

public type ASTTopLevelDefAttrNode struct : IVisitable {
    compose public ASTNode node
    public ASTAttrLstNode* attrLst = nil<ASTAttrLstNode*>
}

public p ASTTopLevelDefAttrNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTTopLevelDefAttrNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitTopLevelDefinitionAttr(this);
}


// ======================================================== LambdaAttrNode =======================================================

public type ASTLambdaAttrNode struct {
    compose public ASTNode node
    public ASTAttrLstNode* attrLst = nil<ASTAttrLstNode*>
}

public p ASTLambdaAttrNode.ctor(CodeLoc codeLoc) {
    this.node = ASTNode(codeLoc);
}
public f<Any> ASTLambdaAttrNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitLambdaAttr(this);
}


// ========================================================= CaseConstantNode ====================================================

public type ASTCaseConstantNode struct : IVisitable {
    compose public ASTExprNode node

}

public p ASTCaseConstantNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTCaseConstantNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitCaseConstant(this);
}


// ========================================================= ReturnStmtNode ======================================================

public type ASTReturnStmtNode struct : IVisitable {
    compose public ASTStmtNode node

}

public p ASTReturnStmtNode.ctor(CodeLoc codeLoc) {
    this.node = ASTStmtNode(codeLoc);
}
public f<Any> ASTReturnStmtNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitReturnStmt(this);
}


// ========================================================= BreakStmtNode =======================================================

public type ASTBreakStmtNode struct : IVisitable {
    compose public ASTStmtNode node

}

public p ASTBreakStmtNode.ctor(CodeLoc codeLoc) {
    this.node = ASTStmtNode(codeLoc);
}
public f<Any> ASTBreakStmtNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitBreakStmt(this);
}


// ======================================================== ContinueStmtNode =====================================================

public type ASTContinueStmtNode struct : IVisitable {
    compose public ASTStmtNode node

}

public p ASTContinueStmtNode.ctor(CodeLoc codeLoc) {
    this.node = ASTStmtNode(codeLoc);
}
public f<Any> ASTContinueStmtNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitContinueStmt(this);
}


// ======================================================= FallthroughStmtNode ===================================================

public type ASTFallthroughStmtNode struct : IVisitable {
    compose public ASTStmtNode node

}

public p ASTFallthroughStmtNode.ctor(CodeLoc codeLoc) {
    this.node = ASTStmtNode(codeLoc);
}
public f<Any> ASTFallthroughStmtNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitFallthroughStmt(this);
}


// ========================================================= AssertStmtNode ======================================================

public type ASTAssertStmtNode struct : IVisitable {
    compose public ASTStmtNode node

}

public p ASTAssertStmtNode.ctor(CodeLoc codeLoc) {
    this.node = ASTStmtNode(codeLoc);
}
public f<Any> ASTAssertStmtNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitAssertStmt(this);
}


// ========================================================= AssignExprNode =======================================================

public type ASTAssignExprNode struct : IVisitable {
    compose public ASTExprNode node

}

public p ASTAssignExprNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTAssignExprNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitAssignExpr(this);
}


// ======================================================== TernaryExprNode ======================================================

public type ASTTernaryExprNode struct : IVisitable {
    compose public ASTExprNode node

    public IVisitable* condition = nil<IVisitable*>
    public IVisitable* trueExpr = nil<IVisitable*>
    public IVisitable* falseExpr = nil<IVisitable*>
    public bool isShortened = false
}

public p ASTTernaryExprNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTTernaryExprNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitTernaryExpr(this);
}


// ======================================================= LogicalOrExprNode =====================================================

public type ASTLogicalOrExprNode struct : IVisitable {
    compose public ASTExprNode node

    public Vector<IVisitable*> operands
}

public p ASTLogicalOrExprNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTLogicalOrExprNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitLogicalOrExpr(this);
}


// ======================================================= LogicalAndExprNode ====================================================

public type ASTLogicalAndExprNode struct : IVisitable {
    compose public ASTExprNode node

    public Vector<IVisitable*> operands
}

public p ASTLogicalAndExprNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTLogicalAndExprNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitLogicalAndExpr(this);
}


// ======================================================= BitwiseOrExprNode =====================================================

public type ASTBitwiseOrExprNode struct : IVisitable {
    compose public ASTExprNode node

}

public p ASTBitwiseOrExprNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTBitwiseOrExprNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitBitwiseOrExpr(this);
}


// ======================================================= BitwiseXorExprNode ====================================================

public type ASTBitwiseXorExprNode struct : IVisitable {
    compose public ASTExprNode node

}

public p ASTBitwiseXorExprNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTBitwiseXorExprNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitBitwiseXorExpr(this);
}


// ======================================================= BitwiseAndExprNode ====================================================

public type ASTBitwiseAndExprNode struct : IVisitable {
    compose public ASTExprNode node

}

public p ASTBitwiseAndExprNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTBitwiseAndExprNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitBitwiseAndExpr(this);
}


// ======================================================== EqualityExprNode =====================================================

public type ASTEqualityExprNode struct : IVisitable {
    compose public ASTExprNode node

}

public p ASTEqualityExprNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTEqualityExprNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitEqualityExpr(this);
}


// ======================================================= RelationalExprNode ====================================================

public type ASTRelationalExprNode struct : IVisitable {
    compose public ASTExprNode node

}

public p ASTRelationalExprNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTRelationalExprNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitRelationalExpr(this);
}


// ========================================================= ShiftExprNode =======================================================

public type ASTShiftExprNode struct : IVisitable {
    compose public ASTExprNode node

}

public p ASTShiftExprNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTShiftExprNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitShiftExpr(this);
}


// ======================================================== AdditiveExprNode =====================================================

public type ASTAdditiveExprNode struct : IVisitable {
    compose public ASTExprNode node

}

public p ASTAdditiveExprNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTAdditiveExprNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitAdditiveExpr(this);
}


// ===================================================== MultiplicativeExprNode ==================================================

public type ASTMultiplicativeExprNode struct : IVisitable {
    compose public ASTExprNode node

}

public p ASTMultiplicativeExprNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTMultiplicativeExprNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitMultiplicativeExpr(this);
}


// ========================================================== CastExprNode =======================================================

public type ASTCastExprNode struct : IVisitable {
    compose public ASTExprNode node

}

public p ASTCastExprNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTCastExprNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitCastExpr(this);
}


// ====================================================== PrefixUnaryExprNode ====================================================

public type ASTPrefixUnaryExprNode struct : IVisitable {
    compose public ASTExprNode node

}

public p ASTPrefixUnaryExprNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTPrefixUnaryExprNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitPrefixUnaryExpr(this);
}


// ====================================================== PostfixUnaryExprNode ===================================================

public type ASTPostfixUnaryExprNode struct : IVisitable {
    compose public ASTExprNode node

}

public p ASTPostfixUnaryExprNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTPostfixUnaryExprNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitPostfixUnaryExpr(this);
}


// ========================================================= AtomicExprNode ======================================================

public type ASTAtomicExprNode struct : IVisitable {
    compose public ASTExprNode node

}

public p ASTAtomicExprNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTAtomicExprNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitAtomicExpr(this);
}


// =========================================================== ValueNode =========================================================

public type ASTValueNode struct : IVisitable {
    compose public ASTExprNode node

}

public p ASTValueNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTValueNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitValue(this);
}


// ========================================================== FctCallNode ========================================================

public type ASTFctCallNode struct : IVisitable {
    compose public ASTExprNode node

}

public p ASTFctCallNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTFctCallNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitFctCall(this);
}


// ==================================================== ArrayInitializationNode ==================================================

public type ASTArrayInitializationNode struct : IVisitable {
    compose public ASTExprNode node

}

public p ASTArrayInitializationNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTArrayInitializationNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitArrayInitialization(this);
}


// ==================================================== StructInstantiationNode ==================================================

public type ASTStructInstantiationNode struct : IVisitable {
    compose public ASTExprNode node

}

public p ASTStructInstantiationNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTStructInstantiationNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitStructInstantiation(this);
}


// ========================================================= LambdaBaseNode ======================================================

public type ASTLambdaBaseNode struct/*: IVisitable*/ {
    compose public ASTExprNode node
}

public p ASTLambdaBaseNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}

// ========================================================= LambdaFuncNode ======================================================

public type ASTLambdaFuncNode struct : IVisitable {
    compose public ASTLambdaBaseNode node

    public ASTLambdaAttrNode* lambdaAttr = nil<ASTLambdaAttrNode*>
    public bool hasParams = false
    public IVisitable* paramLst = nil<IVisitable*>
    public IVisitable* body = nil<IVisitable*>
    public IScope* bodyScope = nil<IScope*>
}

public p ASTLambdaFuncNode.ctor(CodeLoc codeLoc) {
    this.node = ASTLambdaBaseNode(codeLoc);
}
public f<Any> ASTLambdaFuncNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitLambdaFunc(this);
}

public f<String> ASTLambdaFuncNode.getScopeId() {
    return String("lambda:") + this.codeLoc.toString();
}


// ========================================================= LambdaProcNode ======================================================

public type ASTLambdaProcNode struct : IVisitable {
    compose public ASTLambdaBaseNode node

    public ASTLambdaAttrNode* lambdaAttr = nil<ASTLambdaAttrNode*>
    public bool hasParams = false
    public IVisitable* paramLst = nil<IVisitable*>
    public IVisitable* body = nil<IVisitable*>
    public IScope* bodyScope = nil<IScope*>
}

public p ASTLambdaProcNode.ctor(CodeLoc codeLoc) {
    this.node = ASTLambdaBaseNode(codeLoc);
}
public f<Any> ASTLambdaProcNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitLambdaProc(this);
}

public f<String> ASTLambdaProcNode.getScopeId() {
    return String("lambda:") + this.codeLoc.toString();
}


// ========================================================= LambdaExprNode =======================================================

public type ASTLambdaExprNode struct : IVisitable {
    compose public ASTLambdaBaseNode node

    public bool hasParams = false
    public IVisitable* paramLst = nil<IVisitable*>
    public IVisitable* lambdaExpr = nil<IVisitable*>
    public IScope* bodyScope = nil<IScope*>
}

public p ASTLambdaExprNode.ctor(CodeLoc codeLoc) {
    this.node = ASTLambdaBaseNode(codeLoc);
}
public f<Any> ASTLambdaExprNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitLambdaExpr(this);
}

public f<String> ASTLambdaExprNode.getScopeId() {
    return String("lambda:") + this.codeLoc.toString();
}


// ====================================================== FunctionDataTypeNode ===================================================

public type ASTFunctionDataTypeNode struct {
    compose public ASTExprNode node

}

public p ASTFunctionDataTypeNode.ctor(CodeLoc codeLoc) {
    this.node = ASTExprNode(codeLoc);
}
public f<Any> ASTFunctionDataTypeNode.accept(IAbstractAstVisitor* visitor) {
    return visitor.visitFunctionDataType(this);
}


#[test, test.name="ASTNodes Smoke test"]
public f<bool> testASTNodesSmoke() {
    const CodeLoc loc = CodeLoc(3l, 7l, "ast.spice");

    // Base node
    ASTNode base = ASTNode(loc);
    assert base.codeLoc.line == 3l;
    assert base.codeLoc.col == 7l;
    assert base.parent == nil<ASTNode*>;

    // Stmt node composes a base node + carries the 'unreachable' flag
    ASTStmtNode stmt = ASTStmtNode(loc);
    assert stmt.node.codeLoc.line == 3l;
    assert !stmt.unreachable;
    stmt.unreachable = true;
    assert stmt.unreachable;

    // Expression node composes a base node
    ASTExprNode expr = ASTExprNode(loc);
    assert expr.node.codeLoc.col == 7l;

    // Top-level def node composes a base node
    ASTTopLevelDefNode topDef = ASTTopLevelDefNode(loc);
    assert topDef.node.codeLoc.line == 3l;

    // Function-def base composes a top-level def
    ASTFctDefBaseNode fctBase = ASTFctDefBaseNode(loc);
    assert fctBase.node.node.codeLoc.line == 3l;

    // FctDef composes the FctDef-base
    ASTFctDefNode fctDef = ASTFctDefNode(loc);
    assert fctDef.node.node.node.codeLoc.line == 3l;

    // QualifierNode carries a QualifierType
    ASTQualifierNode qual = ASTQualifierNode(loc);
    assert qual.qualifierType == QualifierType::NONE;
    qual.qualifierType = QualifierType::CONST;
    assert qual.qualifierType == QualifierType::CONST;

    // TypeLstWithEllipsisNode
    ASTTypeLstWithEllipsisNode typeLstEllipsis = ASTTypeLstWithEllipsisNode(loc);
    assert typeLstEllipsis.node.codeLoc.line == 3l;

    // TopLevelDefAttrNode
    ASTTopLevelDefAttrNode tlAttr = ASTTopLevelDefAttrNode(loc);
    assert tlAttr.node.codeLoc.line == 3l;

    return true;
}
