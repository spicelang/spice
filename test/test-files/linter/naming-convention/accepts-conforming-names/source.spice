int MAX_RETRIES = 3;

type Point struct {
    double x
    double y
}

f<int> addTwo(int a, int b) {
    return a + b;
}

f<int> main() {
    printf("%d, %d\n", addTwo(1, 2), MAX_RETRIES);
    return 0;
}
