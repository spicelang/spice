// Forward-declaring as a struct and then defining as an interface must be rejected,
// not silently accepted (which would later trip an internal assertion in the type checker).

forward type Foo struct;

type Foo interface {
    p doSomething();
}

f<int> main() {
    return 0;
}
