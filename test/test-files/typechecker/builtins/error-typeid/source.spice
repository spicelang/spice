f<int> main() {
    typeid(); // No template types and no args
    typeid<short>(8s); // Template types and args
    typeid<double, int>(); // Too many template types
    typeid(1, 2); // Too many args
}