import "a";
import "b";

f<int> main() {
    // Cross-file type-identity check: the field `a.b` (typed bfwd::B*) must be
    // assignable from a `B*` resolved through `b.spice`. They must share a typeId.
    B b;
    A a;
    a.b = &b;
    printf("A.b == &b: %d\n", a.b == &b);
}
